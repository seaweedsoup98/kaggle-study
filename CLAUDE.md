# kaggle-study — 세션 작업 규칙

이 저장소는 **여러 Claude 세션이 동시에** 작업한다.
아래 규칙은 세션끼리 서로의 작업을 덮어쓰거나, 남이 만든 파일을 영문도 모른 채
커밋하는 일을 막기 위한 것이다.

## 1. 레인(lane) — 디렉터리 소유권

세션 하나는 **레인 하나**를 맡고, 자기 레인 밖에는 쓰지 않는다.

| 레인 | 쓰기 권한 | 읽기 전용 |
|---|---|---|
| **EDA / 분석** | `<대회>/eda/` | `<대회>/experiments/*/outputs/` |
| **실험 / 모델링** | `<대회>/experiments/<실험명>/` | `<대회>/eda/`, `<대회>/data/` |
| **인프라 / 문서** | `<대회>/data/`, `README.md`, `.gitignore`, `CLAUDE.md` | 전부 |

**한 파일에 두 세션이 쓰지 않는 것**이 핵심이다.
`README.md` 는 특히 충돌이 잦으니, 자기 레인에 해당하는 절만 수정한다.

## 2. 산출물은 단방향

`experiments/*/outputs/` 는 실험 레인이 **생산**하고 다른 레인이 **소비**한다. 역방향은 금지.

소비하는 쪽이 깨지지 않도록, 실험 산출물에는 항상 다음을 함께 남긴다.

- `metrics.json` — 설정과 결과 수치
- `run.log` — 실행 로그
- 재사용 가능한 중간 산출물(OOF 예측 `.npy` 등)

## 3. 새 폴더를 만들면 한 줄이라도 남긴다

`experiments/` 아래에 새 폴더를 만들 때는 **무엇을 · 왜 · 어떤 입력으로** 하는지를
`README.md` 나 스크립트 docstring 에 적는다.
설명 없는 폴더가 갑자기 나타나면 다른 세션이 작업을 멈추고 물어봐야 한다.

## 4. git

- **`git add -A` 금지.** 항상 경로를 명시해서 add 한다
  (다른 세션이 만든 파일을 검토 없이 커밋하게 된다)
- 커밋 전 `git status` 로 **내가 만들지 않은 파일이 섞였는지** 확인한다
- 내 레인이 아닌 파일이 변경되어 있으면 **커밋하지 말고 사용자에게 알린다**
- 브랜치를 쓴다면 레인별 접두어: `eda/*`, `exp/*`, `infra/*`

## 5. 결론이 서로 어긋날 때

레인마다 다른 규모·조건으로 실험하므로 **결론이 충돌할 수 있다**.
이때 어느 한쪽을 지우지 말고 다음 순서로 처리한다.

1. 두 결과가 **정말 모순인지** 확인한다 (신뢰구간이 겹치면 모순이 아니라 "둘 다 미확정"이다)
2. 조건 차이를 찾는다 (표본 크기, 모델, 그룹 수)
3. **작은 실험 쪽에 후속 메모를 붙인다.** 원래 관측치와 판단은 남겨 둔다 —
   왜 재현되지 않았는지가 결과 자체보다 중요한 경우가 많다

> 실제 사례: `eda/03` 의 pseudo labeling 파일럿(40그룹·QDA, +0.0011)이
> `experiments/pseudo_labeling/` 의 전체 실험(512그룹·6모델)에서 재현되지 않았다.
> 파일럿을 지우는 대신 7절에 '후속' 상자를 붙여 두었다.

## 6. 자원

- 무거운 작업(전체 학습, 노트북 실행)은 **동시에 두 개 이상 돌리지 않는다.**
  같은 머신을 공유하므로 둘 다 느려진다
- 데이터는 kagglehub 캐시를 공유한다 (`data/loader.py` 가 경로를 찾아 준다).
  다시 내려받지 말 것 — train/test 각 ~1.3GB

## 7. 노트북

- `eda/` 의 노트북은 **학습용 자료**다. 각 절을
  `무엇을 하나 → 왜 하나 → 원리 → 기대 결과 → 관찰` 순서로 쓴다
- **실행 결과를 포함한 상태로 커밋**한다 (`jupyter nbconvert --to notebook --execute --inplace`)
- 기대와 다른 결과가 나와도 **지우지 않는다.** 기각된 가설에 `❌` 를 붙여 남긴다
